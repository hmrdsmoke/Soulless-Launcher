# Rust build output
/target/
**/target/

# Flatpak build output
/build-dir/
.flatpak-builder/

# Vendored dependencies (regenerated on demand)
/vendor/

# OS / editor cruft
.DS_Store
*.swp
*~

# Vendored, patched crates (see Cargo.toml [patch] section) — must be tracked
!/vendor/
build.log
